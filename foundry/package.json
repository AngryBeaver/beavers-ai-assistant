{
  "name": "beavers-voice-transcript",
  "title": "Beaver's Voice Transcript",
  "version": "1.0.1",
  "description": "Exposes an API for external tools to interact with Foundry Journals.",
  "devDir": "c:\\vtts\\v13.348\\Data\\modules",
  "main": "src/beavers-voice-transcript.js",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "node build.mjs build",
    "watch": "node build.mjs watch",
    "clean": "node build.mjs clean",
    "devbuild": "node build.mjs dev",
    "devwatch": "node build.mjs devwatch",
    "release": "node build.mjs zip",
    "format": "prettier --write \"src/**/*.ts\"",
    "format:check": "prettier --check \"src/**/*.ts\"",
    "tag": "git tag -d ${npm_package_name}_${npm_package_version} 2>/dev/null ; git tag ${npm_package_name}_${npm_package_version} && git push -f origin ${npm_package_name}_${npm_package_version}"
  },
  "devDependencies": {
    "@league-of-foundry-developers/foundry-vtt-types": "^13.346.0-beta.20250812191140",
    "@types/archiver": "^7.0.0",
    "@types/jest": "^30.0.0",
    "archiver": "^7.0.1",
    "chokidar": "^4.0.3",
    "esbuild": "^0.25.1",
    "prettier": "^3.8.1",
    "typescript": "^5.9.3"
  }
}
